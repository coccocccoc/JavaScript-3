// 8번

// 두 수를 입력받아 뺄셈 결과를 반환하는 함수
// 매개변수: 두 수
// 반환값: 결과
const substract = function (x, y) {
  return x - y;
}

// 함수 호출
// 매개변수 개수에 따라 인자를 전달
// 함수의 결과를 저장
const result = substract(5, 3);
console.log(result); // 2